console.log (Hello)

const calculateSquareArea = (width, height) => {
    return width * height 
}

calculateSquareArea(25, 25)

let convertFahrenheitToCelcius = () => {
    return (farenheit -32) * 5 / 9;
}

convertFahrenheitToCelcius(90);

var isEven = (number) => { 
    if(number % 2 === 0) {
    console.log (number_is_even)
    }
}

isEven(6);

let concatenateStrings = (str1, str2) => {
    return str1 + str2;
}

concatenateStrings("Hello, my name is", " Aldrich")